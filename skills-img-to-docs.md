# Skill 본문 — `/img-to-docs`

> 실습 폴더 `claude-lecture-workspace/.claude/skills/img-to-docs/SKILL.md` 로 배포할 완성형.
> 견적서·표 형식 이미지를 읽어 마크다운으로 변환한다.

---

```markdown
---
name: img-to-docs
description: 이미지 파일(견적서, 영수증, 표가 포함된 문서 캡처 등)을 읽어 핵심 데이터를 마크다운 표와 본문으로 추출한다. 입력 폴더의 모든 이미지를 일괄 처리할 수 있다.
---

# /img-to-docs

이미지 → 구조화된 마크다운 변환 Skill.

## 입력
- 단일 이미지 경로 (예: `10_input/quote-1.png`)
- 또는 폴더 경로 (예: `10_input/`) → 폴더 안 모든 이미지 일괄 처리
- 지원 포맷: PNG, JPG, JPEG, WEBP

## 출력
- 단일 입력: `20_extracted/{원본파일명}.md`
- 폴더 입력: `20_extracted/{원본파일명}.md` 를 이미지 수만큼 생성
- 마크다운 본문 구성:
   1. **문서 종류** (견적서 / 영수증 / 명세서 등 자동 판정)
   2. **메타 정보** (발행일, 문서번호, 발신·수신자)
   3. **핵심 데이터 표** (항목 / 수량 / 단가 / 금액)
   4. **합계 정보** (소계, 세금, 총액)
   5. **비고 / 결제 조건**
   6. **검수 포인트** (자동 추출이 어려운 부분 표시)

## 절차

1. **입력 분류**
   - 입력이 파일이면 단일 처리, 폴더면 `*.png|jpg|jpeg|webp` 모두 수집
   - 출력 폴더 `20_extracted/` 가 없으면 생성

2. **이미지 분석 (각 파일마다 반복)**
   - 이미지를 읽어 문서 종류를 먼저 판별
   - 메타 정보 추출 (날짜는 `YYYY-MM-DD` 로 정규화)
   - 표 영역 인식 → 행/열 단위로 데이터 추출
   - 금액은 콤마 제거한 정수로 추출 + 원본 표기도 보존

3. **마크다운 작성 규칙**
   - 표는 GitHub Flavored Markdown 표 사용
   - 금액 컬럼은 우측 정렬 (`|---:|`)
   - 인식 신뢰도가 낮은 셀은 `⚠️` 표시 + 검수 포인트에 기록

4. **검수 포인트 자동 채우기**
   - 흐릿하거나 잘린 영역
   - 합계와 항목 합산이 맞지 않는 경우
   - 부가세 계산 검증 (`subtotal × 0.1 ≈ tax` 일치 여부)

5. **결과 보고**
   - 처리한 파일 수 / 성공 / 실패 요약
   - 실패한 파일은 사유와 함께 목록 출력

## 출력 마크다운 템플릿

````markdown
# {문서종류} — {원본파일명}

## 메타 정보
- 문서번호: {No}
- 발행일: {YYYY-MM-DD}
- 발신: {company_from}
- 수신: {company_to}

## 항목

| 품목 | 수량 | 단가 | 금액 |
|------|---:|---:|---:|
| {item_1} | {qty} | {unit_price} | {amount} |

## 합계
- 소계 (VAT 별도): **{subtotal}**
- 부가세 (10%): **{tax}**
- 총 합계: **{total}**

## 비고
{notes}

## 🔍 검수 포인트
- [ ] {point_1}
- [ ] {point_2}
````

## 실패 시 동작

- 이미지를 못 읽음 → 해당 파일은 건너뛰고 마지막 보고에 "처리 실패" 목록
- 표가 없는 일반 텍스트 이미지 → 본문만 추출하고 "표 없음" 표기
- 손글씨 비중이 높음 → "손글씨 다수, 정확도 제한" 경고를 마크다운 상단에 표시

## 호출 예시

- `/img-to-docs 10_input/quote-1.png`
- `/img-to-docs 10_input/`
- `/img-to-docs ./영수증_4월/`

## 후속 작업으로 이어지는 Skill

- `/approval-doc` — 추출된 견적 데이터 → HTML 품의서
- `/excel-clean` — 추출된 표 데이터를 엑셀로 통합

```

---

## 🧪 강의 시 시연 시나리오

1. `10_input/sample-quote-1.png` 호출 → 결과 확인
2. 일부러 흐릿한 이미지 추가 → 검수 포인트가 정확히 표시되는지
3. 폴더 단위 호출 (`/img-to-docs 10_input/`) → 일괄 처리 시연
4. `/approval-doc` 으로 자연 연결

## ⚠️ 강사 주의

- 실습용 이미지는 모두 `dummy-assets-guide.md` 의 더미만 사용
- 학생이 실제 사내 견적서를 올리지 않도록 1교시에 한 번 더 강조
