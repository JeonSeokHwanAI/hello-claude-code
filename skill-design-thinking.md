# Skill 설계 사고 과정

> Week 3 강의의 핵심 자료. "이런 Skill은 어떻게 만드는가" 에 대한 4단계 설계 사고를
> 강사가 직접 만든 **AI Blog Writer** 의 `/search`·`/review`·`/report` 를 케이스 스터디로 풀어봅니다.

---

## 🎯 한 줄 정리

> **Skill 만들기 = "내가 평소 하는 일을 글로 적어 AI에게 넘긴다."**

거창하지 않아요. 글솜씨가 좋을 필요도 없습니다. **명확하면 됩니다.**

---

## 🧠 4단계 설계 사고

```
1. 관찰        2. 쪼개기      3. 입출력 정의   4. 매뉴얼 작성
─────────     ─────────     ──────────────  ──────────────
"내가 매번    "이걸 단계로     "각 단계마다    "AI가 그대로
 하는 일이     나누면?"         뭐 들어가고      따라할 수 있게
 뭐지?"                         뭐 나와야 해?"   글로"
```

### 1단계 — 관찰 (Observe)

**질문**: "내가 매번 같은 식으로 반복하는 일이 뭔가?"

> 💡 케이스 스터디: **AI Blog Writer 가 만들어진 출발점**
>
> "내가 좋아하는 블로거 글을 매번 손으로 카피해서 분석 노트를 만든다.
> 글을 5개 보고, 공통 패턴을 찾고, 어떤 점이 좋고 나쁜지 표를 만든다.
> ... 이게 매주 2~3시간씩 걸린다."

**체크 포인트**:
- 일주일에 2번 이상 하는 일인가?
- 매번 같은 형식으로 끝나는가?
- 결과물이 비슷한 모양인가?

세 가지가 다 ✅ 이면 Skill 후보 확정.

### 2단계 — 쪼개기 (Decompose)

**질문**: "이걸 누가 봐도 따라할 수 있게 단계로 나누면?"

> 💡 AI Blog Writer 의 분해
>
> ```
> 블로거 분석하기
>   └─ ① 글 5개 수집하기            → /search
>   └─ ② 글들을 12가지 항목으로 분석  → /review
>   └─ ③ 분석을 보기 좋은 리포트로    → /report
>   └─ ④ 그 사람의 톤 추출하기        → /persona
> ```
>
> 한 번에 다 하지 않습니다. **단계가 곧 Skill 단위**.

**체크 포인트**:
- 단계 사이에 **검수 지점**을 둘 수 있는가? (오류 잡기)
- 한 단계를 다른 일에 재사용할 수 있는가? (`/search` 는 분석 말고 클리핑에도 쓸 수 있음)
- 단계가 5개를 넘어가면 → 더 쪼개거나 묶어야 함

### 3단계 — 입출력 정의 (Define I/O)

**질문**: "각 단계마다 무엇이 들어가서 무엇이 나와야 하나?"

> 💡 AI Blog Writer 의 `/search` 입출력
>
> | | 입력 | 출력 |
> |---|------|------|
> | 무엇 | 블로거 별명 + 글 개수 | JSON 파일 (글 본문 + 메타데이터) |
> | 어디 | 사용자 명령어 | `output/{blog_id}/posts_*.json` |
> | 형식 | 텍스트 1줄 | 표준 JSON 스키마 |
> | 실패 시 | "별명 없음" / "네트워크 오류" | 명확한 에러 메시지 |

**체크 포인트**:
- 입력이 모호하면? → "별명이 없으면 사용자에게 물어본다" 처럼 기본 동작 정의
- 출력 형식이 다음 단계 입력과 맞는가? (`/search` 출력 → `/review` 입력)
- 결과물이 어디 저장되는지 명시되어 있는가?

### 4단계 — 매뉴얼 작성 (Write SKILL.md)

**질문**: "AI가 처음 봐도 따라할 수 있게 적었나?"

```markdown
---
name: blog-search
description: 네이버 블로거 별명을 받아 최근 글 N개를 수집하고 JSON 으로 저장한다.
---

# 블로그 글 수집 Skill

## 입력
- 블로거 별명 (예: "리리", "셀슈머") 또는 블로그 ID
- 수집 개수 (생략 시 기본값 5)

## 절차
1. 별명을 ID로 매칭 (config/blogs.json 참조, 없으면 사용자에게 확인)
2. 최근 글 N개를 수집 (각 글의 제목·본문·작성일·URL)
3. 결과를 output/{blog_id}/posts_{YYYYMMDD}.json 으로 저장
4. 수집 결과 요약 1줄로 보고 (글 개수, 가장 최근 글 제목)

## 출력
- 파일: output/{blog_id}/posts_{YYYYMMDD}.json
- 콘솔: "총 5개 수집 완료. 최신글: '..."

## 실패 시
- 별명 매칭 안 됨 → 사용자에게 정확한 ID 묻기
- 네트워크 오류 → 3회 재시도, 그래도 실패면 보고
- 빈 결과 → "글이 없거나 비공개일 수 있음" 안내
```

**체크 포인트** (좋은 Skill 5원칙):
- [ ] 입력/출력 위치가 명시됨
- [ ] 절차가 번호로 나뉨
- [ ] 모호한 부분에 예시·기본값 있음
- [ ] 실패 케이스 동작이 정의됨
- [ ] description 한 문장으로 "언제 호출할지" 분명

---

## 🔄 Command vs Skill — 같은 도구, 다른 형식

`/search`·`/review`·`/report` 는 현재 강사 도구에서 **command (Python 스크립트)** 로 만들어져 있습니다. 이걸 **Skill (마크다운)** 로 바꾸면 무엇이 달라질까요?

| | Command (현재) | Skill (마크다운) |
|---|----------------|-----------------|
| 형식 | Python 코드 | 자연어 + 마크다운 |
| 수정 | 코드 편집 필요 | 글 수정만으로 변경 |
| 작성자 | 개발자 | 누구나 |
| AI 위임 | 명시한 코드 그대로 | AI가 절차를 보고 유연하게 실행 |
| 실패 회복 | 에러 → 멈춤 | AI가 상황 보고 다른 방법 시도 |
| 속도 | 빠름 | 느림 (AI 호출 비용) |

> **언제 Command, 언제 Skill?**
>
> - 정밀하고 빨라야 한다 → Command (스크립트)
> - 유연하게 적응해야 한다 → Skill (AI에게 글로 위임)
> - 둘 다 필요 → Skill에서 Command 호출 (`Bash` 도구 사용)

**Week 3 실습 아이디어**: 강사 도구의 `/search` Python 코드를 보고, 같은 일을 하는 Skill을 마크다운으로 작성해 보기. 두 결과를 비교하면 차이가 와닿습니다.

---

## 🚀 학생용 가이드 — 본인 Skill 만들 때

### 좋은 Skill 후보 신호 5가지

1. ✨ "이거 또 해야 돼?" 하는 일 (반복성)
2. ✨ 중간에 헷갈려서 매번 다시 검색하는 일 (절차 모호)
3. ✨ 결과물이 비슷한 양식으로 나오는 일 (출력 패턴)
4. ✨ 시간이 오래 걸리지만 머리는 안 쓰는 일 (자동화 가치)
5. ✨ 다른 사람이 보면 따라할 수 없을 만큼 본인만의 노하우가 있는 일 (페르소나 가치)

### 흔한 실수와 회피법

| 실수 | 회피 |
|------|------|
| description이 "도와줘" 같이 모호 | "X를 받아 Y를 만든다" 형식으로 |
| 절차 1번에 너무 많은 일 | 한 번에 한 가지만, 5단계 이내로 |
| 출력 위치 명시 안 함 | 폴더·파일명 규칙 항상 적기 |
| "잘 정리해줘" 같은 추상 표현 | 무엇이 잘 정리된 것인지 정의 |
| AI에게 "알아서" 떠넘김 | 모호한 부분엔 기본값·예시 |

### 30분 안에 1개 만들기 (강의 실습 시간 가이드)

```
0:00 - 0:05  관찰 — 후보 1개 선정 + 한 줄 요약
0:05 - 0:10  쪼개기 — 1~5단계로 분해
0:10 - 0:15  입출력 — 각 단계 입출력을 표로
0:15 - 0:25  매뉴얼 작성 — SKILL.md 본문
0:25 - 0:30  호출 + 디버깅 1번
```

---

## 🎓 Week 3 강사 가이드

### 1교시 라이브에서 보여줄 것

1. 2주차에 쓴 `/search` 의 **CLAUDE.md 매뉴얼 부분**을 화면에 띄우기
2. "이거 보세요. 이게 사실은 그냥 잘 쓴 글이에요" 한마디
3. 4단계 설계 사고를 이 도구에 역으로 대입
   - "강사가 처음에 어떻게 관찰했을까?"
   - "왜 4단계로 쪼갰을까? 5개로 안 하고?"
   - "왜 별명을 ID로 먼저 바꾸는 걸 1번 단계로 뒀을까?"
4. **수강생 반응 끌어내기**: "여러분도 이렇게 만들 수 있어요. 같이 하나 만들어 봅시다."

### 흔한 막힘 + 코칭 멘트

- **"제 일은 너무 복잡해서 Skill로 안 돼요"**
  → "복잡한 게 아니라 안 쪼갠 거예요. 1번부터 적어 봅시다."

- **"이거 하면 AI가 잘못 이해해요"**
  → "description 한 문장이 모호할 때 그래요. '~를 받아서 ~를 만든다' 형식으로 다시 써보죠."

- **"제 톤이 안 살아요" (블로그·페르소나 케이스)**
  → "샘플이 부족한 겁니다. 본인 글 1~2개를 samples/ 에 두고 'samples/ 의 톤을 따른다' 한 줄을 절차에 추가하세요."

---

## 📌 다음 단계

이 사고 과정을 익히면:
- 본인 업무를 보는 눈이 달라집니다 ("이건 Skill 후보네")
- Week 4에서 이걸 Agent로 묶을 때, **무엇을 한 Agent로 묶을지** 직관이 생깁니다
- 강의 후에도 새 Skill을 혼자 만들 수 있습니다 — 강의의 진짜 목표
