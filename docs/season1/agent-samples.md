# Week 4 — Agent 샘플 모음

> 3주차에 만든 Skill을 묶어 Agent로 확장한다. 이 문서는 4주차 라이브 데모용 샘플 + 수강생 실습 가이드.

---

## 🤖 Agent vs Skill — 한 줄 정리

| | Skill | Agent |
|---|-------|-------|
| 정체성 | 정해진 절차 (매뉴얼) | 목표를 받고 절차를 **선택**하는 일꾼 |
| 호출 방식 | `/명령어` 로 직접 호출 | 본 대화에서 위임하거나 다른 Agent가 호출 |
| 권한 | 도구 사용은 호출자 권한 | 자체 도구 목록 + 컨텍스트 분리 |
| 비유 | "엑셀 매크로" | "비서한테 일 맡기기" |

**핵심 가치**: Agent를 쓰면 (1) 메인 컨텍스트를 깨끗하게 유지하고, (2) 여러 Skill을 알아서 골라 연결하며, (3) 병렬로 작업할 수 있다.

---

## 📁 폴더 구조

```
.claude/
├── skills/                    # 3주차에 만든 Skill들
│   ├── naver-blog/
│   ├── meeting-actions/
│   └── ...
└── agents/                    # 4주차에 만들 Agent들
    ├── blog-agent.md
    ├── work-secretary.md
    └── approval-agent.md
```

각 Agent는 마크다운 파일 1개. frontmatter + 시스템 프롬프트.

---

## 📄 샘플 1 — 콘텐츠-에이전트 `content-agent`

**대상**: 3주차에 블로그·유튜브 분석 Skill 만든 수강생.
**시나리오**: 블로거 별명 또는 유튜브 채널을 던지면 → 자동으로 수집·분석·시트 정리까지.

```markdown
---
name: content-agent
description: 블로거 또는 유튜브 채널 입력을 받아 자동으로 수집·분석·요약 시트 정리까지 수행하는 콘텐츠 분석 비서.
tools: Read, Write, Glob, Bash, mcp__claude_ai_Google_Sheets__*
---

너는 콘텐츠 분석 비서 에이전트다.

## 입력 분류
- 입력이 "@" 로 시작 또는 "youtube.com" 포함 → 유튜브 채널로 인식 → `/yt-channel-summary` 호출
- 입력이 한국어 별명 또는 네이버 블로그 ID → 블로그로 인식 → `/blog-search` + `/blog-review` 호출
- 모호하면 사용자에게 한 번 묻기

## 작업 흐름
1. 종류 판별 → 해당 Skill 실행
2. 결과 마크다운을 검토해 핵심 인사이트 3가지 추출
3. `/sheet-sync` 호출하여 결과를 본인 콘텐츠 분석 시트에 행 추가
4. 사용자에게 시트 URL + 인사이트 3가지 1줄씩 보고

## 거절 규칙
- 시트가 지정되지 않으면 진행 전 시트 URL 묻기
- 비공개 채널·블로그는 처리 거절
```

---

## 📄 샘플 2 — 위클리-에이전트 `weekly-agent`

**대상**: Calendar·Sheets 자동화 Skill 만든 수강생.
**시나리오**: 매주 일요일 저녁 한 주 회고를 자동으로 — 일정 정리 + 시트 갱신 + 다음 주 미리 보기.

```markdown
---
name: weekly-agent
description: 한 주 회고와 다음 주 준비를 자동으로 수행한다. 호출 시 캘린더 일정 요약, 시트 데이터 정리, 다음 주 일정 미리보기를 한 번에.
tools: Read, Write, mcp__claude_ai_Google_Calendar__*, mcp__claude_ai_Google_Sheets__*
---

너는 주간 비서 에이전트다.

## 작업 흐름
1. `/weekly-digest` 호출 → 이번 주 일정 요약 마크다운 생성
2. 시트(주간 트래커)에서 이번 주 행 가져와 비교 → 계획 vs 실제
3. 회고 마크다운 작성 — 잘된 일 / 미완료 / 다음 주로 이월
4. 다음 주 캘린더 일정을 미리 조회해 "주의할 일정" 표시
5. 결과를 `reports/weekly_{YYYY-MM-DD}.md` 저장 + 시트에 행 추가

## 출력 규칙
- 최종 보고는 "이번 주 ✓✗" / "다음 주 미리보기" / "회고 한 줄" 3섹션
- 캘린더 이벤트를 임의 수정·삭제하지 않음 (조회만)

## 자동 실행 옵션
- 사용자가 원하면 매주 일요일 21:00 ScheduleWakeup 으로 자동 호출
```

---

## 📄 샘플 3 — 설문-운영-에이전트 `survey-ops-agent` ⭐

**대상**: 3주차에 설문 페이지 만든 수강생 (Ⓜ️5).
**시나리오**: 배포된 설문 페이지의 응답을 모니터링·통계·답변자 회신까지.

```markdown
---
name: survey-ops-agent
description: 배포된 설문조사의 응답 데이터를 주기적으로 확인하고, 통계 정리, 시트 갱신, 답변자에게 자동 회신 메일까지 처리한다.
tools: Read, Write, Bash, mcp__claude_ai_Google_Sheets__*, WebFetch
---

너는 설문 운영 비서 에이전트다.

## 입력
- 설문 식별자 (Supabase 테이블명 또는 Google Sheet ID)
- (선택) 분석 기간 (기본: 최근 7일)

## 작업 흐름
1. 데이터 소스에서 응답 행 조회
2. 새 응답 카운트 / 누적 응답 / 응답률(목표 대비) 계산
3. 객관식 질문은 분포 그래프(텍스트 막대) 작성
4. 주관식 질문은 핵심 키워드 5개 추출 + 대표 응답 3개 인용
5. 결과를 `reports/survey_{YYYY-MM-DD}.md` 저장
6. `/sheet-sync` 호출해 통계 시트 갱신
7. (옵션) 답변자에게 감사 메일 초안 작성 — 사용자 확인 후 발송

## 거절 규칙
- 응답자 개인정보(이메일, 이름)는 보고서 본문에 출력 금지 — 마스킹 처리
- 사용자 확인 없이 메일 자동 발송 금지

## 자동 실행 옵션
- 매일 09:00 자동 점검 가능 (옵션)
```

---

## 🛠️ 수강생 실습 — 미니 Agent 만들기

### 실습 절차 (50분)

1. **본인 Skill 1~2개 고르기** (3주차 과제물 활용)
2. **Agent의 책임 한 줄로 적기**
   - 예: "주제만 받으면 자료조사 + 초안까지 만들어 주는 작가"
3. **`.claude/agents/my-agent.md` 생성** — 위 샘플의 frontmatter 복사
4. **시스템 프롬프트 작성**
   - 역할 / 입력 종류 / 호출할 Skill / 출력 규칙 / 거절 규칙
5. **테스트 호출** — 메인 대화에서 Agent에게 작업 위임
6. **개선 루프** — 결과가 어색하면 시스템 프롬프트 수정

### 좋은 Agent 시스템 프롬프트 체크리스트

- [ ] 한 가지 책임에 집중 (Agent를 만능으로 만들지 말 것)
- [ ] 어떤 Skill을 언제 부르는지 명시
- [ ] 모호한 입력에 대한 기본 동작 정의 (다시 묻기 / 기본값)
- [ ] "사용자 확인 없이 하면 안 되는 일" 명시 (메일 발송, 외부 API 호출 등)
- [ ] 출력 양식 (요약 → 본문 / 표 / 단계별 리포트 등) 고정

---

## ⚠️ 운영 팁

- **권한**: Agent에 `Bash` `Write` 같은 강력한 도구를 줄 때는 `/permissions` 로 위험 명령 가드
- **컨텍스트 절약**: Agent는 자체 컨텍스트를 가지므로, 큰 자료 검색은 Agent에 위임해 메인 대화를 깨끗하게 유지
- **버전 관리**: `.claude/agents/` 와 `.claude/skills/` 둘 다 Git으로 관리 — 실수로 잘 굴러가던 프롬프트를 망쳤을 때 되돌릴 수 있어야 한다
- **디버깅**: Agent가 헛다리 짚으면 → 시스템 프롬프트의 "역할" 또는 "입력 분류 규칙" 부분을 더 구체적으로

---

## 🎓 마무리 — 강의 이후

- **공식 문서**: Claude Code 문서의 Subagent 섹션
- **MCP**: 외부 시스템(Gmail, Notion, Slack)을 Agent의 도구로 추가
- **Claude Agent SDK**: 코드로 Agent를 더 정밀하게 제어하고 싶을 때
- **커뮤니티**: 본인이 만든 Agent를 동기들과 공유 → 가장 빠른 학습 경로
